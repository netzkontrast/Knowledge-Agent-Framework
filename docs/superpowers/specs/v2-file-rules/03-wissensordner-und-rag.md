# Per-File Rules — Wissensordner & RAG

**File:** `langdock-deploy/knowledge/03-wissensordner-und-rag.md`
**Owns (canonical facts):** CANONICAL RAG facts: ~2.000-char chunks, 1536-dim, k=50, 1 chunk/doc/query, caps 1.000/200, 256MB.

## Watch / loop focus
Exemplar file — keep pristine. Other files must match these numbers.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
