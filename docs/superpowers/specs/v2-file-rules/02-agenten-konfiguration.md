# Per-File Rules — Agenten-Konfiguration

**File:** `langdock-deploy/knowledge/02-agenten-konfiguration.md`
**Owns (canonical facts):** Agent config facts: 40.000-char system prompt, 20×255 starters, 50 memory, Auto Mode.

## Watch / loop focus
~25 triggers need (Quelle:). UWG: §5 = irreführende Handlungen, §5a = Kennzeichnung/Unterlassung. Dedup S-AK shadow-AI / deprecation / prompt-versioning pairs.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
