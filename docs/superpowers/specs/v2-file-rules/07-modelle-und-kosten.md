# Per-File Rules — Modelle & Kosten

**File:** `langdock-deploy/knowledge/07-modelle-und-kosten.md`
**Owns (canonical facts):** CANONICAL prices: direct EUR/1M (Input/Output), NO multipliers (Quelle: research/13-verified).

## Watch / loop focus
10 triggers still need (Quelle:). Verify prices still current vs langdock.com/models each loop. No 'Multiplikator' language may return.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
