# Per-File Rules — System Prompt (not in knowledge/)

**File:** `langdock-deploy/AGENT_PROMPT.md`
**Owns (canonical facts):** Init protocol (forced anchor searches), modes, refusal strings, gestaffeltes Antwortformat.

## Watch / loop focus
≤15 000 chars (true chars). Keep all three anchor search strings. Reflect against final knowledge each pass.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
