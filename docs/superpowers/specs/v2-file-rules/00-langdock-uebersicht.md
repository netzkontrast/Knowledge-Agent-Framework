# Per-File Rules — Overview / Einstieg

**File:** `langdock-deploy/knowledge/00-langdock-uebersicht.md`
**Owns (canonical facts):** High-level platform overview; entry scenarios (S-LU).

## Watch / loop focus
Integration count must read '55+ (~754 Actions)'. No hard EU-AI-Act dates (verify-pattern). Add (Quelle:) to factual triggers. M-method scenarios (S-LU-001..007) must apply their lens invisibly.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
