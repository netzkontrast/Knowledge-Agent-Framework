# Per-File Rules — Data-Anweisungen

**File:** `langdock-deploy/knowledge/13-data-agent-anweisungen-pro-thema.md`
**Owns (canonical facts):** Per-Thema communication directives (## Data-Anweisung). Anchor '**Data-Anweisung <Thema>'.

## Watch / loop focus
Price refs valid (cite 07). ≥10 Data-Anweisung blocks. Keep anchor lines.

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
- (loop 2) Verified green; price refs cite 07 (now valid). Anchor lines intact.
