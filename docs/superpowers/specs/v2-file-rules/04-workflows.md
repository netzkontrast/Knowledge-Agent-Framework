# Per-File Rules — Workflows

**File:** `langdock-deploy/knowledge/04-workflows.md`
**Owns (canonical facts):** Workflow facts: 2.000-Schritte-Stopp, Budget-Guard pro Lauf, triggers.

## Watch / loop focus
~10 triggers need (Quelle:). Newsletter/Pressemitteilung = workflow/distribution angle (qualify noun).

## Decisions & residual TODOs (append during each pass)
- (loop 1) seeded from gap log; deep pass pending.
